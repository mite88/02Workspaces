package ex13interface.figure;

//가로(밑변),세로(높이)를 멤버로 가지는 클래스 (삼,사각형)
public class FigureData {
	int width, height;

	public FigureData(int width, int height) {
		this.width = width;
		this.height = height;
	}
	
}
